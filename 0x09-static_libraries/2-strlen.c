#include "main.h"
#include <string.h>
/**
 * _strlen - function declared
 * @s: initialized integer
 * Return: the length of a string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
