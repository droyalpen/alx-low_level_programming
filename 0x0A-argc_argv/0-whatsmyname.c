#include <stdio.h>

/**
 * main - prints the program name
 * @argc - the number of arguements passed into the function
 * @argv -pointers to strings
 *
 * Return: always 0
 */

int main(int argc_attribute0_((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
